datasets>=4.5.0
huggingface-hub>=1.8.0
tiktoken>=0.12.0
tqdm>=4.67.3

# Intel macOS (x86_64): PyTorch stopped shipping builds after 2.2.2, which
# also caps Python at 3.12 and requires NumPy 1.x.
torch>=2.2.2; sys_platform == "darwin" and platform_machine == "x86_64"
numpy>=1.26,<2; sys_platform == "darwin" and platform_machine == "x86_64"

# Everything else (Apple Silicon, Linux, Windows): modern torch + NumPy.
torch>=2.8.0; sys_platform != "darwin" or platform_machine != "x86_64"
numpy>=2.0.2; sys_platform != "darwin" or platform_machine != "x86_64"
